= form_for @tag_form, url: question_tags_path do |f|
  - if @tag_form.errors.any?
    #error_explanation
      %h2= "#{pluralize(@tag_form.errors.count, "error")} prohibited this tag from being saved:"
      %ul
        - @tag_form.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
